#include<iostream>
#include<array>
using namespace std;
int main()
{
    char binary[16];
  gets(binary);
    cout<<" "<<binary;
   int n=binary.size();
    cout<<"The Length of the Array is : "<<n;
 return 0;
}
